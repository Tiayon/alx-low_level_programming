#include <stdio.h>
/**
 * main - entry point
 * return: always 0 (success)
 */
int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
	{
		putchar(n);
	}
	putchar('\n');
	/* code to print single digit numbers*/
	return (0);
}
