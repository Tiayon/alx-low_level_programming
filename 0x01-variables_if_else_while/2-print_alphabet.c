#include <stdio.h>
/**
 * main - entry point
 * REturn: Always 0 (success)
 */
int main(void)
{
	char l;
	char a;
	char z;

	for (l = a; l <= z; l++)
	{
		putchar(l);
	}
	/* inline comment*/

	return (0);
}
